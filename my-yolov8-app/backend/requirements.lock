Flask==3.1.3
flask-cors==6.0.2
Pillow==12.2.0
ultralytics==8.2.0
# NOTE: Torch is intentionally NOT pinned here.
# In CI + Docker we install CPU-only torch/torchvision explicitly to avoid pulling massive CUDA wheels
# that can exceed GitHub Actions disk limits.
gunicorn==23.0.0
python-dotenv==1.2.2
pytest==9.0.3
